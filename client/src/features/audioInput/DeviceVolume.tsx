import React from 'react';
import { Slider, Card, H4, Button, H5, MenuItem } from '@blueprintjs/core';

interface Props {
  deviceId: string | null;
}

export default function DeviceVolume({ deviceId }: Props) {
  //const
  //navigator.mediaDevices.getUserMedia({audio: {deviceId: { exact: 'b295e94b0724de1a75c809e06ac3be56a155fc668c4955f13282a9457368855f'}}}).then(console.log)
  return <div>blargh</div>;
}
